---
/**
 * The homepage.
 *
 * Emblem Round won, so this route is that page and nothing else — imported
 * whole rather than copied, with the review furniture switched off. Astro
 * resolves the canonical from the request, so this renders at
 * https://mozzmusic.com/ with no noindex and no comparison bar.
 *
 * The fifty-one concept pages that got us here still exist at /concepts.
 */
import EmblemRound from './v/emblem-round.astro';
---

<EmblemRound review={false} />
